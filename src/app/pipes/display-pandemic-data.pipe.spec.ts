import { DisplayPandemicDataPipe } from './display-pandemic-data.pipe';

describe('DisplayPandemicDataPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayPandemicDataPipe();
    expect(pipe).toBeTruthy();
  });
});
